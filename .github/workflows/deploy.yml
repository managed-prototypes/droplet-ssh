name: Deploy

on:
  # Allow to run manually
  workflow_dispatch:

jobs:
  build:
    name: Deploy
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      # - name: Test S3 access
      #   env:
      #     AWS_REGION: us-east-1 # doesn't matter for DigitalOcean, but required for AWS cli to work
      #     AWS_ACCESS_KEY_ID: ${{ secrets.DO_S3_ACCESS_KEY }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_S3_SECRET_KEY }}
      #   run: aws s3 ls --endpoint=https://managed-prototypes.ams3.digitaloceanspaces.com

      - name: Terraform init
        env:
          AWS_REGION: us-east-1 # doesn't matter for DigitalOcean, but required for AWS cli to work
          AWS_ACCESS_KEY_ID: ${{ secrets.DO_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_S3_SECRET_KEY }}
        run: terraform init
